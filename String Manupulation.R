# split String
unlist(strsplit("ant.ball.c", "\\."))
library(tau)
tokenize("abc defghk")
# Concatenate
paste("toto","tata",sep=' ')
library(stringr)
str_c("toto","tata",sep=",")
cat(c("a","b","c"), sep = "+")
# Counting the number of characters in a string
nchar("abcdef")
