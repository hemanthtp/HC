options("repos")[[1]][1]
ls("package:brocolors")
ls(getNamespace("brocolors"), all.names=TRUE)

source("D:/Troubleshoot.R")

#find all datasets in the package
data(package = "pow")

#Get help for a package
help(package = "epiR")


colval <- t(col2rgb(crayons))

install.packages("mvbutils")
library(mvbutils)

find.funs("package:brocolors")
methods(cbind)
r <- unclass(lsf.str(envir = asNamespace("brocolors"), all = T))
r[grep("cbind.ts", r)]


ls()
rm("ravi")
